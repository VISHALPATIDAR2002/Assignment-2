#include<stdio.h>
    int main(){
        int a;
        float f;
        double d;
        char c;
        printf("size of a is %d\n",sizeof(a));
        printf("size of f is %d\n",sizeof(f));
        printf("size of d is %d\n",sizeof(d));
        printf("size of c is %d\n",sizeof(c));
        return 0;
    }
